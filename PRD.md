# 보건·안전 채용정보 통합 플랫폼 PRD

> 문서 버전: v1.0  
> 작성일: 2026-08-20  
> 상태: MVP 기획안  
> 배포 환경: Vercel  
> 가칭: **세이프잡(SafeJob)**

---

## 1. 제품 개요

### 1.1 한 줄 소개

여러 출처에 흩어진 국내 **보건관리자·안전관리자 채용공고를 한곳에서 검색, 비교하고 원문 지원 페이지로 이동할 수 있는 채용정보 서비스**다.

### 1.2 배경

보건관리자와 안전관리자 구직자는 채용 포털, 기업 채용 페이지, 공공 채용 사이트 등 여러 출처를 반복해서 확인해야 한다. 같은 공고가 여러 사이트에 중복 노출되거나 직무명이 일관되지 않고, 선임 요건·자격증·고용형태 같은 핵심 조건이 본문에 흩어져 있어 빠른 비교가 어렵다.

본 제품은 두 직군에 특화된 공고 구조와 필터를 제공해 탐색 시간을 줄이고, 새 공고와 마감 임박 공고를 놓치지 않도록 돕는다.

### 1.3 핵심 가치 제안

- 보건관리자와 안전관리자 공고만 모아 불필요한 검색 결과를 줄인다.
- 지역, 경력, 고용형태, 선임 여부, 요구 자격 등 직무 특화 조건으로 빠르게 좁힌다.
- 출처가 다른 공고를 동일한 형식으로 보여주어 한눈에 비교할 수 있다.
- 등록일, 마감일, 원문 출처를 명확히 표시해 공고의 신뢰도와 최신성을 판단하게 한다.
- 지원은 출처의 원문 페이지에서 진행하게 하여 지원 절차를 단순화한다.

---

## 2. 제품 목표

### 2.1 MVP 목표

1. 사용자가 첫 화면에서 두 직군의 최신 채용 현황을 파악할 수 있다.
2. 사용자가 3번 이내의 주요 조작으로 원하는 조건의 공고 목록을 만들 수 있다.
3. 사용자가 공고의 핵심 조건을 목록에서 비교한 뒤 원문 지원 페이지로 이동할 수 있다.
4. 운영자가 허용된 데이터 소스를 통해 공고를 추가·수정·마감 처리할 수 있다.
5. 모바일과 데스크톱에서 동일한 핵심 탐색 경험을 제공하고 Vercel에 안정적으로 배포한다.

### 2.2 성공 지표

초기 출시 후 4주간 아래 지표를 관찰한다. 초기 트래픽이 적을 수 있으므로 절대 수치보다 행동 전환율과 오류율을 우선 평가한다.

| 지표 | 정의 | MVP 목표 |
|---|---|---:|
| 검색/필터 사용률 | 방문 세션 중 검색 또는 필터를 1회 이상 사용한 비율 | 35% 이상 |
| 공고 상세 조회율 | 목록을 본 세션 중 상세 페이지를 연 비율 | 30% 이상 |
| 원문 이동률 | 상세 조회 세션 중 원문 보기 버튼을 누른 비율 | 20% 이상 |
| 결과 없음 비율 | 검색/필터 실행 중 결과가 0건인 비율 | 15% 이하 |
| 중복 공고 노출률 | 동일 채용 건이 별도 카드로 반복 노출된 비율 | 5% 이하 |
| 최신성 준수율 | 활성 공고 중 마지막 확인 시점이 24시간 이내인 비율 | 90% 이상 |
| 주요 페이지 오류율 | 홈·목록·상세 페이지 요청 중 오류 비율 | 1% 미만 |

### 2.3 비목표(초기 범위 제외)

- 사이트 내부 입사지원 및 이력서 제출
- 기업용 채용공고 유료 등록·결제
- 구직자와 채용담당자 간 메시지
- AI 합격 예측, 이력서 평가, 자동 지원
- 네이티브 iOS/Android 앱
- 모든 산업·직무로의 확장
- 이용 허락을 받지 않은 사이트의 무단 크롤링

---

## 3. 전제 및 의사결정

### 3.1 전제

- 주요 사용자는 대한민국에서 보건관리자 또는 안전관리자로 취업·이직하려는 사람이다.
- 서비스 언어는 한국어, 지역과 날짜 표시는 대한민국 기준이다.
- 회원가입 없이 검색·필터·상세 조회·원문 이동을 사용할 수 있다.
- MVP의 즐겨찾기는 브라우저 로컬 저장소에 저장한다. 기기 간 동기화와 회원 기능은 후속 단계에서 검토한다.
- 지원 절차와 공고 본문의 최종 책임 주체는 원문을 게시한 기업 또는 채용 플랫폼이다.

### 3.2 데이터 운영 원칙

- 공개 API, 공공데이터, 기업 제공 피드, 제휴 데이터, 운영자 직접 등록 등 사용 권한이 확인된 데이터만 사용한다.
- 각 공고에 원문 URL, 출처, 최초 수집일, 마지막 확인일을 저장한다.
- 원문 전체를 복제하기보다 탐색에 필요한 구조화 정보와 짧은 요약을 제공한다.
- 삭제·마감 요청을 처리할 수 있는 연락 채널과 운영 절차를 마련한다.
- 출처별 이용약관, robots 정책, 저작권 및 개인정보 관련 요구사항을 출시 전에 검토한다.

---

## 4. 대상 사용자

### 4.1 주 사용자

#### A. 보건관리자 취업 준비생

- 산업위생관리기사, 간호사 등 관련 자격을 보유했거나 준비 중이다.
- 신입 지원 가능 여부와 선임 요건을 중요하게 본다.
- 원하는 지역의 정규직 공고를 빠르게 찾고 싶어 한다.

#### B. 경력 안전관리자

- 건설, 제조, 물류 등 특정 산업 경험이 있다.
- 요구 경력, 현장 위치, 프로젝트 기간, 자격증, 연봉 정보를 비교한다.
- 새 공고와 마감 임박 공고를 자주 확인한다.

#### C. 보건·안전 직무를 함께 탐색하는 사용자

- 보유 자격이나 경력으로 두 직무 모두 지원할 수 있다.
- 직무별 채용 수요와 근무 조건을 한 화면에서 비교하고 싶어 한다.

### 4.2 보조 사용자

#### D. 서비스 운영자

- 공고 데이터의 등록, 수정, 중복 병합, 마감 처리를 담당한다.
- 데이터 오류 신고를 확인하고 출처 및 최신성을 관리한다.

---

## 5. 핵심 사용자 시나리오

### 시나리오 1: 조건에 맞는 최신 공고 찾기

1. 사용자가 홈에 접속한다.
2. `보건관리자` 또는 `안전관리자` 탭을 선택한다.
3. 지역, 경력, 고용형태 등 필요한 필터를 적용한다.
4. 최신순으로 정렬된 결과를 확인한다.
5. 공고 상세를 열어 핵심 조건과 원문 출처를 확인한다.
6. `원문에서 지원하기`를 눌러 새 탭에서 출처 페이지로 이동한다.

### 시나리오 2: 두 직군의 채용 현황 비교

1. 사용자가 홈의 직군별 활성 공고 수와 신규 공고 수를 확인한다.
2. 직군 카드를 선택해 해당 목록으로 이동한다.
3. 동일한 필터 체계로 직군별 결과를 비교한다.

### 시나리오 3: 관심 공고 다시 보기

1. 사용자가 목록 또는 상세에서 북마크 버튼을 누른다.
2. 헤더의 `저장한 공고`에서 저장 목록을 확인한다.
3. 마감된 공고는 마감 상태와 함께 구분해 표시된다.

### 시나리오 4: 잘못된 공고 신고

1. 사용자가 상세 페이지에서 `정보 오류 신고`를 누른다.
2. 오류 유형과 선택 설명을 제출한다.
3. 운영자가 신고 내용을 검토해 수정 또는 마감 처리한다.

---

## 6. 정보 구조

```text
홈 (/)
├─ 통합 공고 목록 (/jobs)
│  ├─ 보건관리자 필터 결과 (/jobs?role=health)
│  ├─ 안전관리자 필터 결과 (/jobs?role=safety)
│  └─ 공고 상세 (/jobs/[id])
├─ 저장한 공고 (/saved)
├─ 서비스 소개 (/about)
├─ 데이터 출처 및 운영 정책 (/sources)
├─ 이용약관 (/terms)
├─ 개인정보처리방침 (/privacy)
└─ 관리자 (/admin, 인증 필요)
   ├─ 공고 관리
   ├─ 데이터 수집 현황
   └─ 오류 신고 관리
```

---

## 7. 화면별 요구사항

### 7.1 공통 헤더

- 로고를 누르면 홈으로 이동한다.
- 주요 메뉴는 `채용공고`, `저장한 공고`, `서비스 소개`로 구성한다.
- 데스크톱에서는 상단 고정 헤더, 모바일에서는 축약된 메뉴를 제공한다.
- 현재 페이지 또는 선택된 메뉴를 시각적으로 구분한다.

### 7.2 홈

#### 목적

서비스의 가치를 즉시 전달하고 최신 공고 탐색을 시작하게 한다.

#### 구성

- 한 줄 가치 제안과 통합 검색창
- `보건관리자`, `안전관리자` 직군 선택 카드
- 전체 활성 공고 수, 오늘/최근 7일 신규 공고 수
- 최신 공고 목록
- 마감 임박 공고 목록
- 주요 지역 빠른 필터
- 데이터 출처 및 마지막 업데이트 시각 안내

#### 검색 동작

- 회사명, 공고 제목, 근무지, 자격 키워드를 검색한다.
- 검색어 제출 시 `/jobs?q=검색어`로 이동한다.
- 검색창에는 직무 중심의 예시 문구를 제공한다.

### 7.3 공고 목록

#### 필수 필터

| 필터 | 옵션 예시 | 선택 방식 |
|---|---|---|
| 직군 | 보건관리자, 안전관리자, 전체 | 단일/전체 |
| 지역 | 시·도, 시·군·구 | 복수 선택 |
| 경력 | 신입, 경력, 경력무관 | 복수 선택 |
| 고용형태 | 정규직, 계약직, 파견·용역, 인턴, 기타 | 복수 선택 |
| 근무형태 | 상주, 현장, 순회, 원격/혼합, 미표기 | 복수 선택 |
| 선임 여부 | 선임 필수, 선임 가능, 해당 없음/미표기 | 복수 선택 |
| 자격 요건 | 산업안전기사, 건설안전기사, 산업위생관리기사, 간호사 등 | 복수 선택 |
| 마감 상태 | 접수 중, 마감 임박 | 복수 선택 |

#### 정렬

- 최신 등록순(기본값)
- 마감 임박순
- 관련도순(검색어가 있을 때)

#### 목록 카드 표시 정보

- 직군 배지
- 공고 제목
- 회사명
- 근무 지역
- 경력과 고용형태
- 주요 자격 요건 최대 3개
- 급여 또는 `회사 내규`/`미표기`
- 등록일과 마감일
- 원문 출처
- 신규 공고, 마감 임박, 중복 통합 등의 상태
- 북마크 버튼

#### 동작 및 상태

- 적용된 필터는 URL 쿼리스트링에 반영해 새로고침과 링크 공유가 가능해야 한다.
- 데스크톱에서는 필터 영역을 목록 왼쪽에, 모바일에서는 바텀시트 또는 전체 화면 패널로 표시한다.
- 필터 전체 초기화 기능을 제공한다.
- 로딩, 결과 없음, 오류 상태를 각각 제공한다.
- 페이지네이션 또는 `더 보기` 방식을 사용한다. MVP에서는 검색 엔진 접근성과 URL 재현성을 위해 페이지네이션을 우선한다.

### 7.4 공고 상세

#### 필수 표시 정보

- 공고 제목, 회사명, 직군, 근무지
- 고용형태, 경력, 학력, 급여
- 등록일, 마감일, 마지막 확인일
- 선임 관련 정보
- 필수·우대 자격 및 자격증
- 주요 업무, 자격 요건, 우대 사항, 근무 조건의 구조화 요약
- 원문 출처와 원문 URL
- 데이터 고지 문구

#### 주요 동작

- `원문에서 지원하기` 버튼은 새 탭으로 연다.
- 원문 이동 이벤트를 분석 도구에 기록한다.
- 북마크 추가/해제를 제공한다.
- 공유 링크 복사 기능을 제공한다.
- `정보 오류 신고` 진입점을 제공한다.
- 마감된 공고는 페이지를 유지하되 상단에 마감 상태를 명확히 표시하고 지원 버튼을 비활성화한다.

### 7.5 저장한 공고

- 브라우저에 저장된 공고를 최신 저장순으로 표시한다.
- 저장 해제 기능을 제공한다.
- 활성 공고와 마감 공고를 시각적으로 구분한다.
- 저장 목록이 비어 있을 때 공고 탐색으로 연결하는 안내를 제공한다.
- 로컬 저장 방식과 기기 간 동기화가 되지 않는다는 점을 안내한다.

### 7.6 관리자

- 인증된 운영자만 접근할 수 있다.
- 공고 목록 조회, 검색, 신규 등록, 수정, 마감 처리가 가능해야 한다.
- CSV 가져오기 또는 허용된 데이터 피드의 동기화 결과를 확인할 수 있어야 한다.
- 중복 후보를 확인하고 대표 공고로 병합할 수 있어야 한다.
- 오류 신고의 상태를 `접수`, `검토 중`, `처리 완료`, `반려`로 관리한다.
- 작업 이력에 운영자, 처리 시각, 변경 항목을 남긴다.

---

## 8. 기능 요구사항

우선순위는 `P0 = 출시 필수`, `P1 = 출시 후 우선 개선`, `P2 = 향후 검토`로 정의한다.

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-01 | 직군별 공고 탐색 | P0 |
| FR-02 | 키워드 검색 | P0 |
| FR-03 | 지역·경력·고용형태·자격 등 복합 필터 | P0 |
| FR-04 | 최신순·마감 임박순 정렬 | P0 |
| FR-05 | 공고 상세 정보 표시 | P0 |
| FR-06 | 원문 지원 페이지 이동 | P0 |
| FR-07 | 등록일·마감일·출처·마지막 확인일 표시 | P0 |
| FR-08 | 종료된 공고 자동/수동 마감 처리 | P0 |
| FR-09 | 모바일 반응형 UI | P0 |
| FR-10 | 운영자 공고 CRUD 및 데이터 검수 | P0 |
| FR-11 | 중복 공고 탐지 및 통합 | P0 |
| FR-12 | SEO용 공개 상세 URL과 메타데이터 | P0 |
| FR-13 | 로컬 북마크 | P1 |
| FR-14 | 공고 오류 신고 | P1 |
| FR-15 | 최근 본 공고 | P1 |
| FR-16 | 이메일/웹 푸시 신규 공고 알림 | P2 |
| FR-17 | 회원가입 및 기기 간 북마크 동기화 | P2 |
| FR-18 | 기업용 직접 공고 등록 | P2 |

---

## 9. 공고 데이터 모델

### 9.1 핵심 필드

| 필드 | 형식 | 필수 | 설명 |
|---|---|---:|---|
| `id` | UUID/string | O | 내부 공고 식별자 |
| `title` | string | O | 공고 제목 |
| `companyName` | string | O | 회사명 |
| `role` | enum | O | `health`, `safety`, `both` |
| `employmentType` | enum | O | 정규직, 계약직 등 |
| `careerType` | enum | O | 신입, 경력, 무관 |
| `careerMinYears` | number/null |  | 최소 경력 연수 |
| `locationSido` | string | O | 시·도 |
| `locationSigungu` | string/null |  | 시·군·구 |
| `workplaceAddress` | string/null |  | 상세 근무지 |
| `workStyle` | enum/null |  | 상주, 현장, 순회 등 |
| `appointmentType` | enum/null |  | 선임 필수, 선임 가능, 미표기 |
| `requiredLicenses` | string[] |  | 필수 자격·면허 |
| `preferredLicenses` | string[] |  | 우대 자격·면허 |
| `salaryText` | string/null |  | 원문 급여 표기 |
| `education` | string/null |  | 학력 요건 |
| `summary` | string | O | 구조화된 짧은 요약 |
| `responsibilities` | string[] |  | 주요 업무 |
| `requirements` | string[] |  | 지원 요건 |
| `preferredQualifications` | string[] |  | 우대 사항 |
| `sourceName` | string | O | 데이터 출처 |
| `sourceUrl` | URL | O | 원문 URL |
| `sourceJobId` | string/null |  | 출처의 공고 식별자 |
| `postedAt` | datetime/null |  | 원문 등록일 |
| `expiresAt` | datetime/null |  | 마감일 |
| `firstSeenAt` | datetime | O | 최초 수집일 |
| `lastVerifiedAt` | datetime | O | 마지막 유효성 확인일 |
| `status` | enum | O | `active`, `expired`, `closed`, `draft` |
| `dedupeKey` | string | O | 중복 탐지용 정규화 키 |

### 9.2 중복 판정 기준

아래 항목을 정규화해 중복 후보 점수를 계산한다.

- 회사명
- 공고 제목
- 직군
- 근무 지역
- 마감일
- 출처 공고 ID 또는 원문 URL

자동 판정의 신뢰도가 낮으면 운영자 검수 대상으로 보내며, 중복 공고는 삭제하기보다 대표 공고에 출처 링크를 묶어 추적 가능성을 유지한다.

### 9.3 마감 처리 기준

- 명시된 마감일이 지나면 `expired`로 전환한다.
- 원문이 삭제되었거나 모집 종료로 확인되면 `closed`로 전환한다.
- 마감일이 없는 상시채용은 마지막 확인일을 기준으로 주기적으로 유효성을 재검사한다.
- 자동 상태 변경은 운영 이력에 남긴다.

---

## 10. 데이터 수집 및 운영

### 10.1 MVP 데이터 유입 방식

1. 사용 허가가 확인된 공개 API 또는 공공데이터
2. 채용 기업·플랫폼이 제공한 제휴 피드
3. 관리자 직접 등록 또는 CSV 업로드

### 10.2 데이터 처리 흐름

```text
허용된 데이터 소스
→ 원본 수집
→ 필드 정규화
→ 직군 분류
→ 중복 후보 탐지
→ 필수값·URL 검증
→ 게시
→ 주기적 유효성 확인
→ 마감/수정
```

### 10.3 운영 요구사항

- 동기화 실패, 필수값 누락, 중복 후보 수를 운영자가 확인할 수 있어야 한다.
- 마지막 성공 동기화 시각을 기록한다.
- 외부 소스 장애 시 기존 데이터를 유지하되 최신성 안내를 갱신한다.
- 원문과 내부 정보가 충돌하면 원문을 우선하며, 사용자가 이를 알 수 있게 고지한다.
- 데이터 수집 주기는 소스 정책과 호출 제한을 따른다. 기본 목표는 1일 1회 이상 갱신이다.

---

## 11. UX/UI 원칙

기존 `DESIGN-notion.md`의 디자인 시스템을 기본 방향으로 사용한다.

- 따뜻한 오프화이트 배경과 흰색 카드로 긴 목록의 피로도를 줄인다.
- 구조적 강조색은 파란색 한 가지로 제한하고 주요 CTA, 링크, 포커스에 사용한다.
- 직군, 신규, 마감 임박 상태는 텍스트·아이콘·배지를 함께 사용하며 색상만으로 구분하지 않는다.
- 목록 카드의 정보 우선순위는 `공고명 → 회사·지역 → 경력·고용형태 → 자격 → 날짜`로 통일한다.
- 데스크톱 최대 콘텐츠 폭은 약 1200px로 두고, 모바일에서는 한 열로 재배치한다.
- 마케팅형 장식보다 빠른 스캔, 필터 사용성, 텍스트 가독성을 우선한다.
- 모든 클릭·터치 대상은 모바일에서 최소 44×44px를 목표로 한다.
- 빈 상태와 오류 상태에는 원인 설명과 다음 행동을 함께 제공한다.

---

## 12. 기술 요구사항

### 12.1 권장 구성

- 프론트엔드/서버: Next.js App Router + TypeScript
- 스타일링: Tailwind CSS 또는 프로젝트 내 CSS 토큰
- 배포: Vercel
- 데이터베이스: 서버리스 환경에 적합한 관리형 PostgreSQL
- 데이터 접근: 타입 안전성을 제공하는 ORM 또는 쿼리 계층
- 예약 작업: Vercel에서 실행 가능한 예약 함수 또는 별도 워커
- 분석: 개인정보를 최소 수집하는 웹 분석 도구
- 오류 추적: 서버·클라이언트 오류 수집 도구

최종 라이브러리와 외부 서비스는 구현 시작 시 비용, 한국 리전 지원, 무료 구간, 데이터 보관 정책을 비교해 확정한다.

### 12.2 Vercel 배포 요구사항

- `main` 브랜치 병합 시 Production 배포가 가능해야 한다.
- Pull Request마다 Preview 배포를 생성한다.
- 개발·프리뷰·운영 환경변수를 분리한다.
- 데이터베이스 비밀키와 관리자 인증 키를 클라이언트 번들에 포함하지 않는다.
- 배포 전 타입 검사와 핵심 테스트를 통과해야 한다.
- 예약 동기화 작업은 중복 실행되어도 결과가 손상되지 않도록 멱등성을 보장한다.

### 12.3 성능

- 홈과 목록의 주요 콘텐츠는 느린 네트워크에서도 먼저 읽을 수 있게 서버 렌더링 또는 정적 재검증을 활용한다.
- 이미지가 필요한 경우 적절한 크기와 지연 로딩을 사용한다.
- 목록 조회는 필요한 필드만 반환하고 페이지 단위로 제한한다.
- 검색·필터에 사용하는 데이터베이스 컬럼에 적절한 인덱스를 둔다.
- Core Web Vitals는 출시 시점의 `Good` 기준 충족을 목표로 한다.

### 12.4 접근성

- WCAG 2.2 AA 수준을 목표로 한다.
- 키보드만으로 검색, 필터, 목록, 상세, 원문 이동을 사용할 수 있어야 한다.
- 입력 요소에 연결된 레이블과 명확한 포커스 표시를 제공한다.
- 본문과 UI 텍스트의 색상 대비를 확보한다.
- 스크린리더가 필터 적용 결과 건수와 오류 상태를 인식할 수 있어야 한다.

### 12.5 SEO

- 공고 상세마다 고유하고 영구적인 URL을 제공한다.
- 제목, 설명, canonical URL, Open Graph 메타데이터를 생성한다.
- `JobPosting` 구조화 데이터는 실제 화면 정보와 일치하는 필드만 사용한다.
- 마감 공고는 즉시 삭제하지 않고 상태를 표시한 뒤, 검색 정책에 따라 색인 제외 여부를 결정한다.
- `sitemap.xml`과 `robots.txt`를 제공한다.
- 검색·필터 조합 URL의 무분별한 색인을 방지한다.

### 12.6 보안 및 개인정보

- MVP 공개 탐색 기능은 개인정보 입력을 요구하지 않는다.
- 오류 신고에서 이메일을 받을 경우 선택 항목으로 두고, 수집 목적과 보유 기간을 고지한다.
- 관리자 페이지는 인증과 권한 검사를 서버에서 수행한다.
- 모든 사용자 입력을 검증하고 출력 시 안전하게 처리한다.
- 외부 URL은 허용된 프로토콜인지 검증한다.
- 관리자 변경 및 데이터 동기화 작업에 감사 로그를 남긴다.
- 비밀값은 환경변수로 관리하고 저장소에 커밋하지 않는다.

---

## 13. 분석 이벤트

개인 식별 정보와 검색어 원문 수집은 최소화한다.

| 이벤트 | 발생 시점 | 주요 속성 |
|---|---|---|
| `view_home` | 홈 진입 | 유입 경로 |
| `search_jobs` | 검색 실행 | 검색 결과 수 |
| `apply_filter` | 필터 적용 | 필터 종류, 결과 수 |
| `view_job_list` | 목록 조회 | 직군, 정렬, 페이지 |
| `view_job_detail` | 상세 조회 | 공고 ID, 직군, 출처 |
| `click_source_apply` | 원문 이동 | 공고 ID, 출처 |
| `save_job` | 공고 저장 | 공고 ID, 직군 |
| `unsave_job` | 저장 해제 | 공고 ID |
| `report_job` | 오류 신고 제출 | 공고 ID, 오류 유형 |
| `empty_results` | 결과 없음 | 적용 필터 종류 |

---

## 14. 예외 및 상태 처리

- **급여 미표기:** 추정하지 않고 `급여 정보 없음`으로 표시한다.
- **마감일 미표기:** `상시채용` 또는 `마감일 미정`을 원문에 따라 구분한다.
- **복수 지역:** 대표 지역을 표시하고 나머지는 `외 N곳`으로 축약한다.
- **두 직무 동시 채용:** `보건·안전` 복합 배지를 사용하고 양쪽 필터 결과에 노출한다.
- **원문 링크 오류:** 지원 버튼 대신 `원문 확인 불가` 상태와 신고 기능을 제공한다.
- **외부 데이터 지연:** 마지막 업데이트 시각과 지연 안내를 표시한다.
- **검색 결과 없음:** 필터 일부 해제, 맞춤법 확인, 전체 공고 보기 행동을 제공한다.
- **브라우저 저장 차단:** 북마크 실패를 안내하되 탐색 기능은 계속 사용할 수 있게 한다.

---

## 15. MVP 수용 기준

### 탐색

- [ ] 사용자가 홈에서 보건관리자 또는 안전관리자 목록으로 이동할 수 있다.
- [ ] 키워드, 직군, 지역, 경력, 고용형태 필터를 조합할 수 있다.
- [ ] 필터와 정렬 상태가 URL에 반영되고 공유한 URL에서 동일하게 복원된다.
- [ ] 결과 건수, 로딩, 결과 없음, 오류 상태가 각각 표시된다.

### 공고 정보

- [ ] 모든 게시 공고에 제목, 회사명, 직군, 지역, 출처, 원문 URL, 마지막 확인일이 있다.
- [ ] 상세 페이지에서 원문으로 이동할 수 있다.
- [ ] 마감 공고는 활성 공고와 명확하게 구분되며 지원 버튼이 비활성화된다.
- [ ] 동일 공고의 명백한 중복이 별도 카드로 반복 노출되지 않는다.

### 운영

- [ ] 운영자가 인증 후 공고를 등록, 수정, 마감 처리할 수 있다.
- [ ] 데이터 동기화 결과와 오류를 확인할 수 있다.
- [ ] 출처와 운영 정책 페이지가 공개되어 있다.

### 품질 및 배포

- [ ] 모바일 360px부터 데스크톱 1440px까지 핵심 기능을 사용할 수 있다.
- [ ] 키보드만으로 핵심 사용자 흐름을 완료할 수 있다.
- [ ] Production 및 Preview 배포가 Vercel에서 정상 동작한다.
- [ ] 비밀 환경변수가 클라이언트와 저장소에 노출되지 않는다.
- [ ] 주요 페이지의 메타데이터, sitemap, robots 설정이 검증된다.

---

## 16. 출시 단계

### Phase 0: 데이터 검증 및 프로토타입

- 사용 가능한 데이터 출처와 이용 조건 확정
- 공고 50~100건으로 데이터 모델 검증
- 홈, 목록, 상세 화면 프로토타입 제작
- 실제 사용자 3~5명으로 필터 명칭과 정보 우선순위 확인

### Phase 1: MVP 구현

- 공개 홈, 목록, 상세
- 키워드 검색과 핵심 필터
- 원문 이동
- 관리자 공고 관리
- 중복 처리와 마감 처리
- SEO, 접근성, 분석 이벤트
- Vercel Preview/Production 배포

### Phase 2: 리텐션 기능

- 로컬 북마크와 최근 본 공고 개선
- 신규 공고 알림
- 회원 계정 및 기기 간 동기화 검토
- 사용자 신고와 운영 자동화 강화

### Phase 3: 공급자 기능

- 기업 인증 및 직접 공고 등록
- 공고 성과 대시보드
- 유료 노출 여부 검토

---

## 17. 주요 리스크와 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| 데이터 이용 권한 불명확 | 서비스 중단, 법적 분쟁 | 허용된 소스만 사용하고 출처별 정책 기록 |
| 공고 최신성 저하 | 사용자 신뢰 하락 | 마지막 확인일 표시, 자동 마감, 동기화 모니터링 |
| 중복 공고 증가 | 탐색 경험 악화 | 정규화 키와 운영자 병합 절차 도입 |
| 직무 분류 오류 | 관련 없는 결과 노출 | 규칙 기반 분류 후 낮은 신뢰도 항목 검수 |
| 초기 공고 수 부족 | 재방문 가치 감소 | 출시 전 최소 공고량과 지역 커버리지 기준 설정 |
| 외부 링크 변경·삭제 | 지원 흐름 중단 | 링크 유효성 점검과 오류 신고 제공 |
| 과도한 필터 복잡도 | 모바일 사용성 저하 | 핵심 필터 우선 노출, 나머지는 상세 필터로 분리 |
| 검색엔진 중복 페이지 | SEO 품질 저하 | canonical, noindex 정책, 제한된 색인 URL 운영 |

---

## 18. 출시 전 확정이 필요한 항목

아래 항목은 구현 착수 전에 제품 책임자가 결정해야 한다.

1. 실제 서비스명과 도메인
2. MVP에서 사용할 데이터 출처 및 각 출처의 이용 허용 범위
3. 출시 시 확보할 최소 활성 공고 수와 지역 범위
4. 북마크를 P0에 포함할지 여부
5. 관리자 인증 방식과 운영자 수
6. 오류 신고 시 이메일 등 개인정보를 받을지 여부
7. 분석·오류 추적·데이터베이스 서비스의 최종 선택
8. 공고 요약을 원문 기반 수동 작성으로 할지, 규칙 기반 자동 추출로 할지 여부

---

## 19. 구현 시작 조건(Definition of Ready)

- 데이터 출처 최소 1개의 사용 권한과 호출·갱신 조건이 확인되었다.
- 핵심 필드 20건 이상을 실제 공고에 매핑해 데이터 모델을 검증했다.
- 홈, 목록, 상세의 모바일·데스크톱 와이어프레임이 승인되었다.
- P0 기능과 MVP 제외 범위가 확정되었다.
- 서비스명, 도메인, 개인정보 수집 범위, 운영 담당자가 정해졌다.
- Vercel 프로젝트와 개발·프리뷰·운영 환경 구성이 준비되었다.

